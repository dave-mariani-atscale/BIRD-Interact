# Copy to .env and fill in real values. .env is gitignored; this file is not.

# AtScale Keycloak client secret (used by create_bird_connections.sh to get
# an access token via the password grant).
ATSCALE_CLIENT_SECRET=

# Postgres password for the BIRD data warehouse user (create_bird_connections.sh).
PG_PASSWORD=
