# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Environment
.env
# Backups of a filled-in .env hold live API keys — never commit one
.env.bak*
.env.*.bak
.venv/
.venv-adk/
.conda-py310/
.conda-pkgs/
.pip-cache/

# IDE
.vscode/
.idea/

# Results & logs
results/
*.log
batch_evaluation.log

# Data (download from HuggingFace, not committed)
bird-interact-lite/
bird-interact-full/

# Local-only provider override
shared/_local_provider.py
shared/_legacy_*.py

# Local-only run scripts (personal task subsets, local endpoints)
scripts/[0-9]*run.sh
scripts/run_*.sh

# Holds a live Apps Script deployment URL + write token
scripts/sheet.py

# Working notes from the ETF/AtScale evaluation — investigation records and a
# per-model change log, not documentation of this harness. Kept locally.
# NOTE: docs/model-change-log.md is listed here but is actually TRACKED — it was
# committed before this rule existed and CLAUDE.md requires it in the same commit
# as any model change. The entry below is inert; don't read it as "untracked".
docs/model-change-log.md
docs/bird-interact-etf-atscale-log.md
docs/q16-explore-columns-investigation.md
docs/result-shape-tip-proposal.md
docs/engine-tickets.md
docs/outbound-followups.md
docs/etf-investigation-plan.md
docs/bird-database-testing-prompt.md
docs/sheet-retry-patch.md

# Personal / development files
Bird_ADK_plan.md
CLAUDE.md
.claude/
.agents/
skills-lock.json
reference_code_paper/

# Editor scratch
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# offline re-grade row caches (large, regenerable)
.regrade_cache_*.pkl
docs/etf-baseline-and-followups-plan.md
