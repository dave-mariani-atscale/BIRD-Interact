# Core
google-adk>=1.0.0
google-genai>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# Database
psycopg2-binary>=2.9.9

# LLM
# Cap below 1.92: from that release litellm ships source-only, and building it
# needs a Rust toolchain new enough for edition2024 crates (Homebrew's cargo 1.83
# is not). 1.91.x still publishes wheels.
litellm>=1.0.0,<1.92

# Utilities
sqlglot>=23.0.0
rich>=13.7.0
mcp>=1.0.0
PyYAML>=6.0
